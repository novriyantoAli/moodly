info:
  name: login-user1
  type: http

http:
  method: POST
  url: https://172.16.0.4:8080/auth/login
  body:
    type: json
    data: |-
      {
        "username": "user1@example.com",
        "password": "password1234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
